// Toast rendering is now handled inside ToastProvider in context/ToastContext.tsx
// This component is kept for backward compatibility but renders nothing.
export default function ToastContainer() {
  return null;
}
